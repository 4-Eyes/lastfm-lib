# Disable emulator audio
machine:
  environment:
    QEMU_AUDIO_DRV: none

dependencies:
  pre:
    - chmod +x gradlew

test:
  pre:
    - emulator -avd circleci-android24 -no-window:
        background: true
        parallel: true
    - circle-android wait-for-boot